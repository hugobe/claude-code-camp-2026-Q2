# Windows Zone.Identifier files (WSL download metadata)
*:Zone.Identifier

# Environment / secrets
.env
.env.local
week0_explore/preview/data/world/

# SQLite WAL-mode runtime files (regenerated automatically, no stable content)
*.db-shm
*.db-wal

#Python cache
*.pyc
